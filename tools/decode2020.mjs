// Re-export the canonical 2020-Design decoder (lives in frontend/src so the
// floorplan importer and the CLI pricers share one implementation).
export { decode2020 } from '../frontend/src/decode2020.js';

/**
 * Scorer v2 — re-export of the ENGINE's designer rubric (AD-3 moved it to
 * eclipse-engine/src/designScore.js so the generate-and-score core ranks
 * candidates with the exact rubric CI ratchets on). One source of truth.
 */
export { scoreKitchenV2, CRAFT_PASS_FRACTION } from '../../eclipse-engine/src/designScore.js';
